================================================================================
RESTANTE - Sistema Concurrente (Trabajo Final Programacion Concurrente)
Chequeo contra "Propuesta de tema para trabajo final"
Fecha: 2026-07-28  |  Rama: master
Version corta: reemplaza al restante anterior, que quedo largo. Lo ya resuelto
se lista una sola vez y sin detalle (el como quedo en soluciones.txt).

LOS TRES ARCHIVOS:
  restante.txt     - ESTE: que falta hacer (el chequeo contra la propuesta)
  soluciones.txt   - que se resolvio y como (el changelog con las razones)
  arquitectura.txt - como funciona el sistema y por que los protocolos son
                     correctos (la parte semantica; es el material del informe)
================================================================================

YA CUMPLIDO DE LA PROPUESTA
  - Buffer limitado en dos variantes (semaforos / monitores) detras de IBuffer.
  - Cache lectores/escritores: 4 variantes (2 politicas x 2 mecanismos).
    CacheMonitoresJusta nueva y cableada.
  - 4 endpoints en la API, uno por combinacion. ConfigurationRunService recibe
    ICache e IBuffer por constructor.
  - Ids unicos (GeneradorIdsOrden), poison pill, instrumentacion de tiempos,
    CalculadoraMetricas, prints fuera del camino medido.

DECISION TOMADA
  - El nombre "Justa" SE MANTIENE: la propuesta (secc. 7) nombra las politicas
    "preferencia a lectores" y "politica justa". En el informe se aclara que el
    mecanismo interno es prioridad a escritores y que "justa" refiere a que
    elimina la inanicion.

--------------------------------------------------------------------------------
LO QUE FALTA, EN ESTE ORDEN (cada punto destraba el siguiente)
--------------------------------------------------------------------------------

1. ARREGLOS CHICOS QUE DESTRABAN LA MEDICION
   [RESUELTO] CacheMonitoresJusta.LeerResumen(): implementado con el mismo
       protocolo PedidoLeer/LiberaLeer que Leer(), recorrido fuera del lock.
   [RESUELTO] CacheSemaforos.LeerResumen(): implementado extrayendo el protocolo
       de lector a EntrarComoLector/SalirComoLector, compartido con Leer().
   [RESUELTO] Leer() con _estados[ordenId] directo en CacheSemaforosJusta y
       CacheSemaforos -> ahora TryGetValue + null, como pide ICache.
   [RESUELTO] Etiquetas de tecnica cruzadas en ApisConcurrentes -> ahora
       SemaforosSinPrioridad y MonitoresJusta.
   [ ] IntervaloLecturaMs hardcodeado (50ms, const) en ConfigurationRunService
       -> pasarlo a ConfiguracionRun; es la perilla para variar la proporcion
       lecturas/escrituras que pide la secc. 7.
   [RESUELTO] Menores: comentario suelto del controller, _colaLectores,
       lectoresDemorados, throw->response en cero en CalculadoraMetricas,
       Folder fantasma Core\Semaforos\ en el .csproj. WeatherForecast.cs ya
       estaba borrado.
   [ ] Enum PoliticasCache: lo referencia Orden.TecnicaCache pero nunca se
       asigna -> decidir si se asigna en el Productor o se borra de ambos lados.
   [ ] OJO: hay un stub de proceso-secuencial en el controller sin firma de
       metodo -> NO COMPILA hasta completarlo o comentarlo (es del punto 2).

2. SOLUCION SECUENCIAL (secc. 7)
   [RESUELTO] Core/ProcesadorSecuencial.cs: un solo hilo, DOS simulaciones por
       orden (igual que productor+consumidor), estados en Dictionary pelado,
       sin buffer ni sincronizacion. Timestamps de buffer quedan null.
   [RESUELTO] Endpoint POST proceso-secuencial (no async), tecnica "Secuencial".
   [RESUELTO] CalculadoraMetricas tolera corridas sin buffer: espera en buffer
       = null (no cero) y procesamiento = CreadoEn -> CompletadoEn.
   PENDIENTE DE VERIFICAR: compilar y correr una vez con pocas ordenes.
   REGLA PARA MEDIR (secc. 7): las corridas concurrentes que se comparen contra
   la secuencial van con CantLectores = 0 (los lectores son el experimento de
   la cache, no de la linea base).

3. PERSISTENCIA SQL SERVER (secc. 4 y 6)
   [ ] Paquete + connection string + tabla de ordenes.
   [ ] Guardado desde el consumidor, DESACTIVABLE por configuracion (o por
       lotes al final de la corrida): la DB dentro del camino medido domina
       los tiempos y arruina la secc. 7.
   [ ] El indicador de correctitud exige "procesada exactamente una vez Y
       persistida" -> el test del punto 4 verifica contra la DB.

4. TESTS AUTOMATIZADOS (secc. 5 y 6)
   [ ] Proyecto xUnit agregado a la .sln.
   [ ] Correctitud: N ingresadas -> N procesadas exactamente una vez, sin
       perdidas ni duplicados, persistidas (pasa / no pasa).
   [ ] Estres (race conditions) y corridas largas (deadlock), parametrizados
       por variante de buffer via IBuffer.
   [ ] Cache: lectores concurrentes de verdad, escritor excluye a todos, y
       politica justa sin inanicion de escritores bajo carga de lectores.

5. MEDICIONES E INFORME (secc. 6 y 7)
   [ ] Barrido: productores, consumidores, tamanio de buffer, volumen
       (1.000/5.000/10.000). Varias corridas por combinacion, promediar y
       descartar la primera (warm-up/JIT).
   [ ] Comparacion de politicas de cache variando la proporcion
       lecturas/escrituras (depende del punto 1).
   [ ] Secuencial vs concurrentes con los tres volumenes (depende del punto 2).
   [ ] Tablas y graficos comparativos + interpretacion.
   [ ] Comparacion cualitativa semaforos vs monitores (secc. 6; entregable
       escrito, no codigo).
   [ ] README (esta vacio) y limpiar obj/ y .vs/ commiteados.
================================================================================
